/**
 * Constants matching Supabase schema (types are not used at runtime in JS).
 */
export const CHARACTER_HEALTH_VALUES = ['very healthy', 'okay', 'dying'];

export const DEFAULT_CHARACTER_IMAGE_URI =
  'https://placehold.co/200x200/E8F5E9/2E7D32?text=🌱';
